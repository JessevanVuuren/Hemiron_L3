export interface ListFunctionResponse {
  // Define properties here based on the response
  id: string;
  name: string;
  projectId: string;
  language: string;
  functionUrl: string;
  status: string;
}
